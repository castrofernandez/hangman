import React from 'react';

import LanguageSelector from './language.selector.jsx';

const Header = ({ }) => (
    <header>
        header
        <LanguageSelector />
    </header>
);

export default Header;
